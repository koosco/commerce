# ===================================================================
# Local Development Environment Variables
# ===================================================================
# Usage: docker compose --env-file .env.local --profile core up -d
# ===================================================================

# Database Configuration
DB_ROOT_PASSWORD=root
DB_USER=admin
DB_PASSWORD=admin1234
DB_HOST=db
DB_PORT=3306

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379

# Kafka Configuration (Docker internal network)
KAFKA_PROFILE=local
KAFKA_ADVERTISED_HOST=host.docker.internal
KAFKA_BOOTSTRAP_SERVERS=kafka:9092

# Monitoring Configuration
PROMETHEUS_CONFIG=prometheus.local.yml
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin123
