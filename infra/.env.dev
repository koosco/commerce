# ===================================================================
# Development (k3d) Environment Variables
# ===================================================================
# Usage: docker compose --env-file .env.dev --profile core --profile kafka up -d
#
# In k3d environment:
#   - Services inside k3d access Docker containers via host.k3d.internal
#   - Kafka data is stored separately in data/kafka_dev
# ===================================================================

# Database Configuration (k3d pods access Docker MariaDB)
DB_ROOT_PASSWORD=root
DB_USER=admin
DB_PASSWORD=admin1234
DB_HOST=host.k3d.internal
DB_PORT=3306

# Redis Configuration (k3d pods access Docker Redis)
REDIS_HOST=host.k3d.internal
REDIS_PORT=6379

# Kafka Configuration (k3d pods access Docker Kafka)
KAFKA_PROFILE=dev
KAFKA_ADVERTISED_HOST=host.k3d.internal
KAFKA_BOOTSTRAP_SERVERS=kafka:9092

# Monitoring Configuration
PROMETHEUS_CONFIG=prometheus.local.yml
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin123
